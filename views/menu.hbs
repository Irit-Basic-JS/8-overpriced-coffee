<ul class="coffeelist">
  {{#each items}}
  <li>
    <header class="coffeename">{{name}}</header>
    <img src="{{image}}" alt="{{name}}">
    <a href="/buy/{{name}}" class="link action">
      В корзину ({{price}} ₽)
    </a>
  </li>
  {{/each}}
</ul>